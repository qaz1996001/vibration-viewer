Start the Tauri development server (frontend + Rust backend).

Run `cargo tauri dev` in the project root. If it fails:
1. Check if port 1420 is already in use
2. Check for Rust compilation errors and fix them
3. Check for missing npm dependencies and run `npm install`

Report the dev server status back to me.
